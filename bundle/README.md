# Nansen MCP for Claude Desktop

This extension connects Claude Desktop to the hosted Nansen MCP server. It runs a bundled, pinned copy of `mcp-remote` and sends your API key in the `NANSEN-API-KEY` header. Claude Desktop asks for the key when you install the extension.

Setup guide: https://docs.nansen.ai/mcp/connecting
